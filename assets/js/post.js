// var editor = document.getElementById('text');

// CKEDITOR.replace( editor,{height: 200 ,width:595});

// // CKEDITOR.instances.editor.document.getBody().getText();
// // CKEDITOR.instances.editor.editable().getText();
// // CKEDITOR.instances.editor.getData();

// function createTextSnippet() {
//     var instance =CKEDITOR.instances.editor.document.getBody().getText();
// instance.updateElement();
// $("#text").attr("value",instance.getData());
//     //return true, ok to submit the form
//     return true;
// }